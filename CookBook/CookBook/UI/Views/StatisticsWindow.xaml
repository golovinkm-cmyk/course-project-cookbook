<Window x:Class="UI.Views.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Статистика" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Статистика рецептов" 
                  FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Фильтры -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Период с:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="StartDatePicker" Width="120" Margin="0,0,15,0"/>
            <TextBlock Text="по:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="EndDatePicker" Width="120" Margin="0,0,15,0"/>
            <Button Content="Применить фильтр" Width="130" Height="30"
                   Click="ApplyFilterButton_Click"/>
            <Button Content="Сбросить" Width="100" Height="30"
                   Margin="10,0,0,0" Click="ResetFilterButton_Click"/>
        </StackPanel>

        <!-- Диаграммы -->
        <TabControl Grid.Row="2">
            <TabItem Header="По категориям">
                <oxy:PlotView x:Name="CategoryPlotView" Margin="10"/>
            </TabItem>

            <TabItem Header="По сложности">
                <oxy:PlotView x:Name="DifficultyPlotView" Margin="10"/>
            </TabItem>

            <TabItem Header="По месяцам">
                <oxy:PlotView x:Name="MonthPlotView" Margin="10"/>
            </TabItem>

            <TabItem Header="Общая статистика">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="Общая статистика" FontSize="16" FontWeight="Bold"
                                  Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Всего рецептов:"
                                      FontWeight="Bold" Margin="0,0,10,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TotalRecipesText"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Общее время приготовления:"
                                      FontWeight="Bold" Margin="0,0,10,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TotalTimeText"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Среднее время приготовления:"
                                      FontWeight="Bold" Margin="0,0,10,5"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="AverageTimeText"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Рецептов в избранном:"
                                      FontWeight="Bold" Margin="0,0,10,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="FavoriteRecipesText"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Премиум рецептов:"
                                      FontWeight="Bold" Margin="0,0,10,5"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="PremiumRecipesText"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
